# 維護紀錄

## 2026-07-02

### 1. 部署到 GitHub Pages

- 專案目標：將已下載到本機的無障礙內容生成工具部署到 GitHub Pages。
- Repo：https://github.com/HuaiyingWang/a11y-generator-claude.git
- 發現本機 `C:\Users\User`（使用者家目錄）被誤建立了一個 git repo（`git rev-parse --show-toplevel` 指向家目錄而非專案資料夾）。該 repo 沒有任何 commit，且未設定 remote，確認無資料遺失風險後已刪除，避免誤將整個家目錄（含 `.ssh`、瀏覽器資料等）提交上去。
- 於專案資料夾 `a11y-generator-gemini-claude` 重新 `git init`，加入 remote `origin`，`git pull` 合併遠端既有的 `README.md`，再將 `index.html`、`worker.js` commit 並 push 到 `origin/master`。
- GitHub Pages 設定方式：Settings → Pages → Source 選 Deploy from a branch → Branch 選 `master` / `/(root)`，網址為 `https://huaiyingwang.github.io/a11y-generator-claude/`。

### 2. Cloudflare Worker（Anthropic API 代理）設定

- `worker.js` 是獨立的 Cloudflare Worker，負責轉發前端請求到 Anthropic API 並處理 CORS，**無法**由 GitHub Pages 執行（GitHub Pages 僅提供靜態內容）。
- 取得 `ANTHROPIC_API_KEY` 步驟：至 console.anthropic.com 建立 API Key，並在 Billing 設定付款方式與用量上限。
- Cloudflare 部署步驟：於 dash.cloudflare.com 建立 Worker → 貼上 `worker.js` 內容 → 部署 → 在 Settings → Variables and Secrets 新增 `ANTHROPIC_API_KEY`（類型須選 **Secret** 加密，不可選 Plaintext）。
- 使用者已部署完成，Worker 網址為 `a11y-generator-claude.love520297.workers.dev`。
- 已將 [index.html](index.html) 內的 `WORKER_URL` 常數（原為 placeholder）更新為實際網址並 push，GitHub Pages 前端已可正確呼叫 Worker。

### 3. 生成結果視覺設計優化（政府／學校適用）

- 問題：使用者測試後回報，工具**生成出來的無障礙內容**（非工具本身操作介面）視覺設計過於單調，希望符合 WCAG AA/AAA 規範的同時，也具備適合政府、學校單位使用的專業質感。
- 原本驅動生成的三組 AI system prompt（文字/圖片生成 `sharedRules`、後台原始碼修正 `htmlSystemBase`、整頁原始碼整理 `fullSystemBase`）已有完整的結構、間距、字級、無障礙規則，但**完全沒有指定實際色彩配置與視覺風格**，導致 AI 每次生成的配色隨機、缺乏一致的品牌調性。
- 新增「視覺設計系統」規則（三處 prompt 皆同步加入第 20 條），內容包含：
  - 主色：`#0369A1`（沉穩藍）／強調色：`#0F172A`（深藏青）
  - 中性色階：文字 `#0F172A`、次要文字 `#334155`、邊框 `#E2E8F0`、區塊底色 `#F8FAFC`
  - 語意狀態色（僅搭配圖示/文字使用，不可單靠顏色傳達）：成功 `#15803D`、警告 `#B45309`、錯誤 `#B91C1C`
  - 統一 8px 圓角、輕量陰影（`0 1px 3px rgba(15,23,42,.08)`），禁止厚重陰影、漸層、霓虹色
  - 禁止使用 emoji 作為視覺圖示，強調符號改用純文字符號（如 ✓、✕）搭配色彩與文字
  - hover/focus 狀態一律使用同色階加深，避免切換色相
- 「整頁原始碼整理」模式（`fullSystemBase`）因為是優化既有頁面，額外加註但書：**若原始碼已有明確一致的品牌色，優先沿用原始品牌色**；僅在原始碼配色混亂或找不到明確品牌色時，才套用上述政府/學校預設配色方案。
- 此變更僅影響 AI 生成內容的樣式指令，不影響工具本身既有的操作邏輯與介面。

### 相關 Commit

| Commit | 說明 |
|--------|------|
| `7fb5283` | Add accessibility generator frontend and Cloudflare Worker proxy |
| `9c61114` | Set Cloudflare Worker URL for API proxy |
| `6ca0dae` | Add government/school visual design system to generation prompts |

### 待辦／後續可關注事項

- [ ] 實際重新測試生成結果，確認新配色套用效果是否符合預期，如需微調色調（更深/更淺/換色）可再調整。
- [ ] 若未來要新增其他機構類型（非政府/學校）的配色方案，可考慮改為下拉選項讓使用者選擇風格。
